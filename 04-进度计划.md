# 04 进度计划（14 天 · 个人独立）

## 一、里程碑

| 里程碑 | 截止 | 验收标准 |
|---|---|---|
| M1 遥测上屏 | D2 晚 | E1 飞控连接→状态面板位置/速度/电量实时刷新 |
| M2 地图可看 | D4 晚 | Canvas 瓦片渲染+缩放平移+实时位置图标 |
| M3 规划闭环 | D6 晚 | 点击/拖拽航点→XML 导入导出→手动编辑 |
| M4 AI 功能 | D8 晚 | 贪婪优化（前后总距离对比）+回归预测出数 |
| M5 功能冻结 | D10 晚 | 图表+日志+设置+双主题+异常矩阵全通过 |
| M6 交付 | D13 | 报告定稿+录屏+答辩彩排完成 |

## 二、逐日排期

| 天 | 任务 | 交付检查点 |
|---|---|---|
| D0 | ① Windows 装 JDK17/21+IDEA+SceneBuilder ② E1 飞控环境准备与连通测试 ③ 抓取南京周边 OSM 瓦片 z=10-14 ④ 与老师确认串口模拟口径 | 飞控遥测数据正常接收；tiles/ 目录就绪 |
| D1 | Maven 工程骨架+JavaFX hello FXML；四区布局静态版；Drone/Waypoint 实体+单测 | mvn javafx:run 出界面 |
| D2 | MavlinkParser（4 类报文+CRC）+UdpLink+状态面板绑定 | M1 |
| D3 | GeoUtil 投影公式+单测；TileLoader+缓存；MapCanvas 基础渲染 | 固定 z 级地图显示 |
| D4 | 滚轮缩放+拖拽平移；实时位置/航点路径叠加 | M2 |
| D5 | 地图点击加航点+列表 CRUD+拖拽同步；ActionMode/优先级编辑 | 规划操作全通 |
| D6 | MissionXmlCodec（XMLEncoder/Decoder）导入导出；坏文件/越界异常矩阵；.plan 转换导入（可选） | M3 |
| D7 | PathOptimizer 贪婪+ConflictChecker+优化前后动画预览 | 总距离对比表出数 |
| D8 | BatteryPredictor 正规方程+训练集构建（飞行日志）+预测界面 | M4 |
| D9 | SensorChart（LineChart+Timer 采样）；CsvLogStore 备份/恢复/筛选 | 曲线与日志闭环 |
| D10 | 设置模块（串口/波特率/频率/缩放/采样间隔持久化）；ThemeManager 双主题；低电量红色闪烁 | M5 |
| D11 | 全链路联调+断线/坏文件/越界异常演练；JUnit 补齐核心类 | 手动清单全过 |
| D12 | 缓冲日（前期滑动补偿）；录屏演示视频 | 备份演示素材 |
| D13 | 课程设计报告定稿+类图/截图补全；答辩 PPT 素材 | M6 |
| D14 | 答辩彩排（预设问题过一遍）；回放模式演练兜底 | 上场 |

## 三、每日自查三问

1. 今天交付物是否可运行？（不可运行=当天未完成）
2. 明天关键路径上有没有被阻塞项？（瓦片/数据/环境类问题不过夜）
3. 当天代码是否提交 git？（每天至少 1 commit，写清信息）

## 四、功能裁剪规则（滑期预案）

优先级保序：遥测显示 > 地图 > 规划+XML > 日志 CSV > 贪婪优化 > 回归预测 > 冲突检测 > 路径动画 > 拍照动作 > .plan 双格式导入。

- 滑到 D8 仍未达 M3：砍 .plan 导入与动画，保 XML 主线
- 滑到 D10 仍未达 M4：回归预测降级为一元(仅距离特征)，冲突检测保留静态校验
- 底线（无论如何保留）：遥测/地图/规划/XML/日志/主题/三类异常处理——均为任务书硬性条款，不参与裁剪

## 五、风险登记

| 风险 | 概率 | 对策 |
|---|---|---|
| 飞控环境搭建问题 | 中 | 优先使用 UDP 连接；另备虚拟串口（com0com）演示串口路径；最终兜底=回放模式 |
| 瓦片下载不完整 | 低 | D0 完成并抽查边缘瓦片；渲染对缺瓦片做占位降级不崩溃 |
| XMLEncoder 与自定义 XML 不一致 | 低 | 以 JavaBeans 结构为准设计实体类，先写 Codec 单测再写 UI |
| JavaFX 依赖下载失败 | 低 | Maven 阿里云镜像 |
| 精度坑（1e7 度/mm/mV 换算） | 中 | 解析器单测含手工构造帧的期望值，防"能跑但数不对" |
