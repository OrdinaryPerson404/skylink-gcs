# SkyLink GCS 类图套件索引

> 教学口径与 `.agent/class-diagram.svg`（课设教学版）一致：全部属性 private（-）、对外仅 public（+）方法、
> 斜体=接口抽象方法、四种关系（关联/聚合/组合/依赖/实现）标注、每图附链路走读。
> 生成源码在 `.agent/uml-src/`（python svggen.py + 各图 gen 脚本），改完重跑脚本即可再生成；
> SVG → PNG 用 Edge 无头渲染：
> `msedge --headless --screenshot=out.png --window-size=W,H file:///路径.svg`

## 概要类图

| 文件 | 内容 |
|---|---|
| SkyLink GCS 概要类图.svg/.png | 全部 55 个运行类分层全景（界面 24 / 业务编排 11 / 实体 4 / 协议 2 / 传输 4 / 工具 3 / 开发辅助 7），主干关系 + 全景走读 |

## 详细类图（核心域，成员级）

| 文件 | 覆盖类 | 教学要点 |
|---|---|---|
| 详细图1-连接管理链路 | ConnectionService / ConnectionManager / CommandLink / LinkChannel / CliLink / MavLinkLink / SerialTransport / UdpTransport（+LiveVehicle/FxSafe 简引用） | 双接口实现（CommandLink + LinkChannel）、★多态调用点、状态机语义（首包转连/3s 看门狗/指数退避）、解锁指令走读 |
| 详细图2-遥测模型与飞行记录 | LiveVehicle（43 只读属性三分组）/ VehicleSnapshot+Builder / AppState / FxSafe / FlightRecorder / Session / DataPage | 写侧收口、不可变快照跨线程、NaN 不覆盖、armed 自动记录、.skylog 文件注记 |
| 详细图3-任务规划与预测域 | PlanPage / RoutePlanner+RouteResult / MissionXml+MissionFile / BatteryPredictor+PredictionResult / LinearRegression / RangeEstimator / Waypoint / PredictDialog | 贪婪最近邻 O(n²)、最小二乘 β=(XᵀX)⁻¹Xᵀy、XML 四重校验+XXE 加固、五特征物理系数 |
| 详细图4-协议层 | CliProtocol（40 正则）/ MavLink（MSG 常量/CRC_EXTRA/学习）+ Parser/PBuf/Frame / 解析 record 群 / MavCodecTest / CliProtocolTest | v1/v2 双协议状态机、CRC_EXTRA 字节累加、运行时学习、黄金帧/黄金样本双护栏 |
| 详细图5-UI 体系 | MainShell / BasePage / 五页面 / TopNav / MapView / widget 群 / ThemeManager/Toast/ConfirmDialog/Icons | 模板方法+多态分发、Canvas 自绘（软投影/姿态仪/瓦片引擎/波形）、looked-up color 换肤 |

## 与旧图的关系

- `class-diagram.puml` + `SkyLink GCS 类图.png/svg`（2026-09-07 旧版）保留未动；
- 本套图为 2026-09-09 新增，口径更新：CliLink/MavLinkLink 实现双接口、CliProtocol 正则数 40、
  补齐 MapView.setVehiclePosition / GeoConvert / GpsHttpServer / 规划预测域等旧图缺失内容。
- 报告（面向对象课程设计实验报告）第 2 章插图 = 概要类图（图2.1）+ 详细图1（图2.2）+ 详细图3（图2.3）。
