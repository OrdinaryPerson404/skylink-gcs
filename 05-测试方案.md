# 05 测试方案

## 一、单元测试（JUnit 5，核心算法类覆盖目标 ≥80%）

| 测试类 | 用例 | 断言要点 |
|---|---|---|
| GeoUtilTest | haversine：南京(32.06,118.79)→北京(39.90,116.40) | 距离 ≈ 900km（±5km） |
| | mercator：32.06N/118.79E @ z=11 | 瓦片号 ≈ (1699, 831)；反向换算往返误差 < 1px |
| WaypointTest | distanceTo 对称性、越界构造（lat=91）抛 IllegalArgumentException | 100m 内两点对称误差 <0.1%；越界必须抛 |
| PathOptimizerTest | 4 点已知布局：优化后总距离 < 原始顺序总距离；2 点平凡序；含 home 闭合 | totalDistance 单调不增；贪心结果与手算一致 |
| ConflictCheckerTest | 两点相距 30m（阈值 50m）检出；总航程 6km vs maxRange 5km 检出；正常序列 0 违规 | 违规清单字段正确 |
| BatteryPredictorTest | 合成数据 y=8+0.6x₁+2x₂−0.4x₃ 加零噪声 | 拟合系数与真值误差 <1e-6；含噪声样本残差有界 |
| MavlinkParserTest | 手工构造 GLOBAL_POSITION_INT 帧（lat=320612345 → 32.0612345°）、SYS_STATUS 帧、坏 CRC 帧 | 数值换算正确；坏帧丢弃且计数+1 |
| MissionXmlCodecTest | 写→读 roundtrip 字段一致；截断 XML 抛特定异常；越界航点读入被拒 | 往返无损；异常类型可定位到"格式错误弹窗" |
| CsvLogStoreTest | 写 100 行→恢复 100 行；故意写 3 行坏数据→跳过 3 行计数；按日期/型号筛选 | 行数与筛选结果正确 |

运行方式：`mvn test`，接入构建，保证回归可持续。

## 二、手动测试清单（演示前必过）

| # | 场景 | 步骤 | 通过标准 |
|---|---|---|---|
| M1 | 正常遥测 | E1 实机连接，切 LOITER/CIRCLE | 状态面板各字段刷新，1-10s 采样间隔生效 |
| M2 | 连接失败 | 未连飞控直接连接 | 弹窗+底部红字，重连 3 次后停止 |
| M3 | 运行中断链 | 飞行中断开连接 | 状态栏提示，UI 不崩溃，可重新连接 |
| M4 | 坏任务文件 | 导入手工截断的 XML | 弹窗含原因，界面原任务不丢 |
| M5 | 坐标越界 | 编辑航点输入 lat=95 | 拦截+提示合法范围 |
| M6 | 航点过近 | 连点两下同一位置 | 列表标红+冲突提示 |
| M7 | 超航程 | 布点超出 maxRange | 冲突提示含数值 |
| M8 | 优化效果 | 8 个乱序航点一键优化 | 总距离下降且有动画预览 |
| M9 | 电池预测 | 输入距离/负载/风速 | 出预测值+曲线，非法输入被拦 |
| M10 | 日志闭环 | 备份→清空→恢复→按日期/型号筛选 | 数据一致，筛选结果正确 |
| M11 | 主题切换 | 白天↔夜间来回切 | 全界面换肤，无残留样式 |
| M12 | 低电量预警 | 遥测电量 <20%（mock 注入） | 数值红色闪烁 |
| M13 | 回放兜底 | 断开飞控，用录制 CSV 回放 | 界面行为与实时一致（答辩预案） |
| M14 | 长跑稳定 | 持续连接 30min | 内存无明显泄漏，曲线不卡顿 |

## 三、缺陷记录模板（报告第 5 章素材）

| 编号 | 日期 | 现象 | 复现步骤 | 根因 | 修复 | 验证 |
|---|---|---|---|---|---|---|

要求：至少收录 5 个有分析价值的缺陷（如字节序换算错误、FX 线程违规、缓存键漏 z 级等），报告里写"根因分析"而不是只写现象。

## 四、策略说明

- "我写你测"分工沿用：我实现核心逻辑，先定接口签名，再补 JUnit
- 不引入 JaCoCo 等覆盖率工具（性价比低），用测试类清单自证覆盖
- 测试优先级：算法类 > 解析/编解码 > 存储类 > UI（UI 以手动清单为主）
